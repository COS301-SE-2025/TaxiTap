// Mock switchDrivertoBoth function for testing
export const switchDriverToBothHandler = async (ctx: any, args: any) => {
  // Mock implementation for testing
  return {
    success: true,
    message: "Driver switched to both roles successfully"
  };
};
